(() => {
  const tree = Array.isArray(window.PREPBASE_KNOWLEDGE_TREE) ? window.PREPBASE_KNOWLEDGE_TREE : [];
  if (!tree.length) return;

  const byId = {
    "business-analysis": [
      "Смысл: бизнес-анализ связывает проблему бизнеса, ожидания заинтересованных сторон, процессы, требования и измеримый результат изменения. Аналитик не просто собирает пожелания, а помогает понять, зачем изменение нужно и какой эффект оно должно дать.",
      "Как применять: начинать с проблемы и цели, затем фиксировать контекст, стейкхолдеров, ограничения, текущий процесс, целевое состояние, требования и критерии приемки.",
      "Пример: бизнес хочет снизить ручную обработку заявок. BA уточняет, где возникает задержка, кто участвует в процессе, какие правила принятия решения есть сейчас, какие метрики должны измениться после автоматизации.",
      "На интервью: проверяют умение отличать цель от решения, работать с конфликтующими ожиданиями, формулировать требования и доказывать ценность через метрики."
    ],
    "system-analysis": [
      "Смысл: системный анализ переводит бизнес-требования в технически реализуемую модель: границы системы, API, данные, интеграции, события, надежность, безопасность и эксплуатационные ограничения.",
      "Как применять: после уточнения сценария описать контекст системы, контракты, модель данных, потоки интеграций, ошибки, мониторинг, нефункциональные требования и план проверки.",
      "Пример: для запуска онлайн-заявки SA проектирует REST API создания заявки, статусы жизненного цикла, событие ApplicationCreated, схему хранения, интеграцию с проверкой клиента и правила повторной отправки.",
      "На интервью: смотрят, умеешь ли ты видеть систему целиком, обсуждать trade-offs, проектировать контракты и не забывать про отказы, идемпотентность, безопасность и наблюдаемость."
    ],
    "ba-requirements-engineering": [
      "Смысл: Requirements Engineering превращает разрозненные ожидания в управляемый набор требований, который можно согласовать, реализовать, проверить и поддерживать после изменений.",
      "Как применять: разделять business, stakeholder, functional, non-functional и transition requirements; каждое требование делать однозначным, проверяемым и связанным с целью.",
      "Пример: вместо 'сделать удобный поиск' зафиксировать: пользователь может искать заявку по номеру, ИНН и телефону; система возвращает результаты за 2 секунды; пустой результат объясняется понятным сообщением.",
      "На интервью: часто спрашивают типы требований, свойства хорошего требования, acceptance criteria, traceability и работу с изменениями scope."
    ],
    "ba-user-story": [
      "Смысл: user story описывает потребность пользователя в компактной форме, но не заменяет анализ сценариев, ограничений и критериев приемки.",
      "Как применять: формулировать 'как <роль> хочу <действие>, чтобы <ценность>', затем добавлять acceptance criteria, исключения, ограничения и ссылку на бизнес-цель.",
      "Пример: 'Как менеджер поддержки, хочу видеть историю обращений клиента, чтобы быстрее понять контекст проблемы'. AC: история отсортирована по дате, видны канал, статус, ответственный и последние комментарии.",
      "На интервью: важно не путать story с задачей разработки и уметь объяснить, когда нужны use case, BPMN или полноценная спецификация."
    ],
    "ba-use-case": [
      "Смысл: use case раскрывает взаимодействие актора и системы от цели до результата, включая основной поток, альтернативы и исключения.",
      "Как применять: описывать акторов, предусловия, trigger, main success scenario, extensions, postconditions и бизнес-правила.",
      "Пример: 'Оформить возврат товара': клиент выбирает заказ, система проверяет срок возврата, предлагает способ возврата, создает заявку и уведомляет склад.",
      "На интервью: проверяют умение находить альтернативные потоки: товар не подлежит возврату, истек срок, заказ не найден, платеж уже отменен."
    ],
    "ba-acceptance-criteria": [
      "Смысл: acceptance criteria задают проверяемые условия, при которых требование считается реализованным и принимаемым бизнесом.",
      "Как применять: писать критерии через Given/When/Then или список проверок, обязательно покрывать позитивные, негативные и граничные сценарии.",
      "Пример: Given клиент без активной подписки, When он нажимает 'Продлить', Then система показывает доступные тарифы и не списывает деньги до подтверждения.",
      "На интервью: частая ошибка — писать критерии как общие пожелания: 'должно быть удобно', 'быстро работает', без измеримого результата."
    ],
    "ba-bpmn-foundation": [
      "Смысл: BPMN дает общую нотацию для описания процесса: кто выполняет шаги, какие события запускают поток, где возникают решения и как системы обмениваются сообщениями.",
      "Как применять: использовать pool для участника процесса, lane для роли, task для действия, gateway для развилки, event для начала/окончания/исключения, message flow для взаимодействия между участниками.",
      "Пример: процесс заявки: клиент отправляет форму, система проверяет данные, при ошибке возвращает уточнение, при успехе создает заявку и отправляет уведомление менеджеру.",
      "На интервью: часто проверяют разницу sequence flow и message flow, корректное использование gateway и умение моделировать исключения, а не только happy path."
    ],
    "ba-cjm": [
      "Смысл: Customer Journey Map показывает путь клиента через этапы, точки контакта, ожидания, эмоции, боли и возможности улучшения.",
      "Как применять: фиксировать сегмент пользователя, цель, этапы пути, каналы, действия, pain points, backstage-процессы и метрики этапов.",
      "Пример: для открытия счета этапы могут быть: выбор продукта, заполнение анкеты, проверка документов, ожидание решения, первый вход в кабинет.",
      "На интервью: важно объяснить, как CJM превращается в backlog: pain point связывается с гипотезой, метрикой и конкретным улучшением."
    ],
    "sa-rest-api": [
      "Смысл: REST API описывает ресурсы и операции над ними через HTTP. Хороший REST-контракт предсказуем для клиента, стабилен при изменениях и явно описывает ошибки.",
      "Как применять: проектировать ресурсы, URI, методы, статусы, схемы request/response, фильтрацию, сортировку, пагинацию, идемпотентность и модель ошибок.",
      "Пример: POST /applications создает заявку и возвращает 201 + id; GET /applications/{id} возвращает состояние; PATCH /applications/{id} изменяет допустимые поля; повторный POST с Idempotency-Key не создает дубль.",
      "На интервью: спрашивают методы HTTP, статус-коды, отличие PUT/PATCH/POST, версионирование, пагинацию, обработку ошибок и почему 200 с ошибкой в body часто плохая идея."
    ],
    "sa-http": [
      "Смысл: HTTP — транспортный протокол большинства API. Для аналитика важны методы, заголовки, статусы, тело запроса, кэширование, TLS и поведение при ошибках.",
      "Как применять: фиксировать метод, URL, query/path параметры, headers, content-type, status codes, timeout, retry policy и требования к безопасности.",
      "Пример: GET /clients?phone=... должен быть безопасным и идемпотентным, а POST /payments запускает бизнес-операцию и требует защиты от повторной отправки.",
      "На интервью: часто проверяют 2xx/4xx/5xx, идемпотентность методов, разницу authentication/authorization и обработку timeouts."
    ],
    "sa-openapi": [
      "Смысл: OpenAPI — машинно-читаемая спецификация HTTP API, которая служит контрактом между аналитиками, разработчиками, тестировщиками и потребителями API.",
      "Как применять: описывать paths, operations, parameters, requestBody, responses, schemas, security, examples и reusable components.",
      "Пример: для POST /applications в OpenAPI фиксируется JSON-схема заявки, обязательные поля, пример запроса, ответы 201/400/409/500 и формат ошибки.",
      "На интервью: важно показать contract-first мышление: сначала согласовать контракт и примеры, затем разрабатывать и тестировать совместимость."
    ],
    "sa-api-errors": [
      "Смысл: модель ошибок делает API понятным для клиентов и поддержки. Ошибка должна объяснять, что произошло, можно ли повторить запрос и как найти инцидент в логах.",
      "Как применять: определить code, message, details, fieldErrors, correlationId, retryable, status и правила локализации сообщения.",
      "Пример: 409 APPLICATION_ALREADY_EXISTS с correlationId и ссылкой на существующую заявку лучше, чем общий 500 'Something went wrong'.",
      "На интервью: проверяют, отличаешь ли бизнес-ошибки от технических и умеешь ли проектировать ошибки для диагностики, а не только для UI."
    ],
    "sa-api-pagination": [
      "Смысл: пагинация ограничивает объем ответа и делает список стабильным по производительности и UX.",
      "Как применять: выбирать offset/page для простых списков или cursor для больших и изменяющихся наборов данных; фиксировать сортировку, limit, total и nextCursor.",
      "Пример: GET /events?limit=100&cursor=abc возвращает items и nextCursor; клиент продолжает чтение без пропуска новых событий.",
      "На интервью: частая ошибка — не учитывать стабильную сортировку и получать дубли/пропуски при изменении данных между страницами."
    ],
    "sa-api-versioning": [
      "Смысл: версионирование API управляет изменениями контракта без внезапной поломки потребителей.",
      "Как применять: различать breaking и non-breaking changes, описывать deprecation policy, срок поддержки и миграционный план.",
      "Пример: добавление optional поля обычно безопасно, а переименование обязательного поля требует новой версии или backward-compatible периода.",
      "На интервью: важно говорить не только 'v1/v2 в URL', а про совместимость, коммуникацию с потребителями и контрактные тесты."
    ],
    "sa-api-auth": [
      "Смысл: authentication отвечает 'кто вызывает API', authorization — 'что ему разрешено делать'. В требованиях нужно фиксировать оба аспекта.",
      "Как применять: описывать способ идентификации, scopes/roles, срок жизни токенов, refresh, аудит действий и поведение при 401/403.",
      "Пример: сервис партнеров вызывает API по OAuth 2.0 client credentials, получает scope applications.write и не может читать персональные данные без отдельного scope.",
      "На интервью: частая ловушка — путать 401 и 403, JWT и сессию, роль и разрешение, access token и refresh token."
    ],
    "sa-graphql": [
      "Смысл: GraphQL позволяет клиенту запросить ровно нужную структуру данных, но переносит часть сложности в схему, resolvers, авторизацию и контроль нагрузки.",
      "Как применять: описывать schema, query, mutation, input types, error format, authorization per field и ограничения глубины/сложности запроса.",
      "Пример: мобильное приложение одним запросом получает клиента, его активные продукты и последние обращения, не делая несколько REST-вызовов.",
      "На интервью: важно назвать trade-offs: меньше overfetching, но сложнее кэширование, безопасность полей и защита от тяжелых запросов."
    ],
    "sa-grpc": [
      "Смысл: gRPC использует protobuf-контракты и хорошо подходит для быстрых service-to-service интеграций, особенно внутри контролируемой инфраструктуры.",
      "Как применять: описывать service, rpc methods, request/response messages, streaming, deadlines, status codes и правила совместимости proto.",
      "Пример: RiskScoringService.CalculateScore принимает ClientProfile и возвращает ScoreResult; deadline 300 ms защищает вызывающий сервис от зависания.",
      "На интервью: спрашивают отличие от REST, плюсы protobuf, streaming и ограничения для браузерных клиентов."
    ],
    "sa-soap": [
      "Смысл: SOAP — XML-based интеграционный стиль, часто встречающийся в enterprise и legacy-системах, где важны WSDL, строгие операции и расширения безопасности.",
      "Как применять: фиксировать operation, XML schema, envelope, fault, endpoint, namespaces и требования к WS-Security, если они есть.",
      "Пример: банковская legacy-система принимает SOAP-операцию CheckClientStatus и возвращает строго типизированный XML-ответ.",
      "На интервью: достаточно понимать WSDL, envelope, SOAP Fault и почему такие интеграции требуют аккуратной спецификации форматов."
    ],
    "sa-idempotency": [
      "Смысл: идемпотентность гарантирует, что повторная обработка одного и того же намерения не создаст повторный бизнес-эффект.",
      "Как применять: вводить idempotency key, business key, deduplication table, TTL и единые правила ответа на повторный запрос.",
      "Пример: повторная отправка POST /payments с тем же Idempotency-Key возвращает результат первого платежа, а не списывает деньги второй раз.",
      "На интервью: это одна из ключевых тем интеграций: повторы неизбежны из-за timeout, сетевых ошибок и неопределенного результата операции."
    ],
    "sa-retries-timeouts": [
      "Смысл: timeout и retry защищают систему от зависаний, но неправильные повторы могут усилить аварию и создать дубли операций.",
      "Как применять: фиксировать timeout budget, retryable errors, max attempts, exponential backoff, jitter и запрет повторов для неидемпотентных операций.",
      "Пример: чтение справочника можно повторить 3 раза с backoff, а создание платежа повторять только при наличии idempotency key.",
      "На интервью: важно говорить про retry storm, cascading failure и связь retry policy с идемпотентностью."
    ],
    "sa-dlq": [
      "Смысл: Dead Letter Queue хранит сообщения, которые не удалось обработать штатно, чтобы не блокировать основной поток и не терять данные.",
      "Как применять: описывать условия попадания в DLQ, формат ошибки, retry count, владельца разбора, алерты и процедуру replay.",
      "Пример: событие ClientUpdated с невалидным ИНН уходит в DLQ после трех попыток, команда получает алерт и исправляет источник данных.",
      "На интервью: проверяют понимание, что DLQ — не мусорная корзина, а операционный механизм восстановления."
    ],
    "sa-saga": [
      "Смысл: Saga управляет бизнес-транзакцией, разбитой на несколько локальных транзакций в разных сервисах, с компенсациями при сбое.",
      "Как применять: описывать шаги, события/команды, состояние saga, компенсирующие действия, timeout и ручное вмешательство.",
      "Пример: бронирование поездки: зарезервировать отель, списать оплату, выписать билет; если оплата не прошла, бронь отеля отменяется.",
      "На интервью: важно объяснить, что saga не дает атомарности ACID, а управляет eventual consistency и компенсациями."
    ],
    "sa-outbox-inbox": [
      "Смысл: Outbox/Inbox помогает надежно публиковать и принимать события, когда нужно согласовать изменение БД и отправку сообщения.",
      "Как применять: писать событие в outbox в той же транзакции, что и бизнес-изменение; отдельный publisher отправляет событие, consumer ведет inbox для deduplication.",
      "Пример: при создании заявки сервис сохраняет заявку и ApplicationCreated в outbox; publisher позже отправляет событие в Kafka.",
      "На интервью: тема часто всплывает рядом с dual-write problem, идемпотентностью и exactly-once мифами."
    ],
    "sa-cdc": [
      "Смысл: CDC передает изменения из базы данных как поток событий, часто без прямого изменения кода приложения.",
      "Как применять: определить таблицы, ключи, формат события, порядок, snapshot, lag, схему удаления и обработку персональных данных.",
      "Пример: изменения таблицы clients читаются Debezium и публикуются в Kafka, а DWH обновляет витрину клиентов почти в реальном времени.",
      "На интервью: важно назвать риски: схема БД становится контрактом, возможны лаги, дубли, порядок и проблемы с удалениями."
    ],
    "sa-sql-basics": [
      "Смысл: SQL нужен аналитику для проверки данных, поиска аномалий, описания выборок и общения с разработкой/QA на языке фактов.",
      "Как применять: уверенно использовать SELECT, JOIN, WHERE, GROUP BY, HAVING, CTE, оконные функции и понимать порядок выполнения запроса.",
      "Пример: найти клиентов с более чем одной активной заявкой: сгруппировать заявки по client_id и отфильтровать HAVING count(*) > 1.",
      "На интервью: часто дают задачи на JOIN, агрегации, дубликаты, NULL, оконные функции и объяснение результата запроса."
    ],
    "sa-transactions": [
      "Смысл: транзакция объединяет несколько изменений в единое логическое действие и задает правила commit/rollback и видимости данных.",
      "Как применять: фиксировать границы транзакции, инварианты, isolation level, блокировки, retry при конфликте и последствия rollback.",
      "Пример: перевод денег должен списать сумму с одного счета и зачислить на другой в рамках согласованной операции; частичное выполнение недопустимо.",
      "На интервью: важно знать ACID, dirty/non-repeatable/phantom reads и почему долгие транзакции могут блокировать систему."
    ],
    "sa-indexes": [
      "Смысл: индекс ускоряет поиск и сортировку, но замедляет запись и занимает место. Аналитик должен понимать, какие запросы будут критичными.",
      "Как применять: описывать частые фильтры, сортировки, кардинальность, уникальность, expected volume и SLA ответа.",
      "Пример: если поиск заявки почти всегда идет по client_id и created_at, нужен индекс, поддерживающий этот паттерн чтения.",
      "На интервью: проверяют понимание, что индекс не магия: важны селективность, порядок колонок и план выполнения."
    ],
    "sa-kafka": [
      "Смысл: Kafka — распределенный лог событий: producer пишет события в topic, consumer group читает их по offset, а partition задает параллелизм и порядок внутри ключа.",
      "Как применять: описывать topic, key, partitioning, retention, consumer groups, schema registry, delivery guarantees, retry/DLQ и правила совместимости событий.",
      "Пример: topic application.status.changed хранит изменения статуса заявки; key = applicationId сохраняет порядок событий по одной заявке.",
      "На интервью: часто спрашивают offset, partition, consumer group, retention, ordering, at-least-once и почему consumer должен быть идемпотентным."
    ],
    "sa-rabbitmq": [
      "Смысл: RabbitMQ — брокер сообщений с exchange, queue и routing key; хорошо подходит для routing, work queues и task distribution.",
      "Как применять: описывать exchange type, binding, queue durability, ack/nack, retry topology, DLQ и prefetch.",
      "Пример: exchange notifications направляет email.sms.push события в разные очереди по routing key notification.email или notification.sms.",
      "На интервью: полезно сравнить с Kafka: RabbitMQ про маршрутизацию сообщений и очереди, Kafka про долговременный лог событий и replay."
    ],
    "sa-event-modeling": [
      "Смысл: event modeling описывает бизнес-факты, которые уже произошли, и показывает, как состояние системы меняется во времени.",
      "Как применять: называть события в прошедшем времени, фиксировать producer, consumers, payload, business meaning и жизненный цикл события.",
      "Пример: ApplicationApproved означает, что заявка уже одобрена; это не команда 'одобрить заявку', а факт, на который реагируют другие системы.",
      "На интервью: проверяют разницу command/event, ownership события и влияние изменения payload на потребителей."
    ],
    "sa-event-contracts": [
      "Смысл: контракт события должен быть стабилен для потребителей так же, как контракт API. Событие без владельца и схемы быстро становится источником поломок.",
      "Как применять: фиксировать event name, version, schema, required/optional fields, semantic meaning, producer, consumers и compatibility rules.",
      "Пример: поле status в ApplicationStatusChanged должно иметь согласованный enum, а добавление нового значения требует уведомления потребителей.",
      "На интервью: важно говорить про backward/forward compatibility, schema registry, consumer-driven contracts и мониторинг lag/errors."
    ],
    "sa-event-sourcing": [
      "Смысл: Event sourcing хранит не текущее состояние, а последовательность событий, из которых состояние можно восстановить.",
      "Как применять: проектировать события как неизменяемые факты, продумывать replay, snapshot, версии событий и исправление ошибочных событий компенсацией.",
      "Пример: баланс счета восстанавливается из AccountOpened, MoneyDeposited, MoneyWithdrawn, FeeCharged, а не хранится только как одно поле.",
      "На интервью: важно назвать плюсы аудита и replay, но также сложность миграций, объем данных и требования к дисциплине событий."
    ],
    "sa-cqrs": [
      "Смысл: CQRS разделяет модель команд и модель чтения, чтобы независимо оптимизировать изменение состояния и быстрые запросы.",
      "Как применять: отделять write model от read model, описывать синхронизацию, eventual consistency, задержку обновления витрины и UX-поведение.",
      "Пример: команда CreateApplication пишет в основную модель, а список заявок в UI читается из отдельной read-модели, обновляемой событиями.",
      "На интервью: важно объяснить trade-off: больше сложности и eventual consistency ради производительности и независимости моделей."
    ],
    "sa-oauth-oidc": [
      "Смысл: OAuth 2.0 решает делегирование доступа, OpenID Connect добавляет слой идентификации пользователя поверх OAuth.",
      "Как применять: описывать flow, client, authorization server, resource server, scopes, access token, refresh token и claims.",
      "Пример: пользователь логинится через authorization code flow, приложение получает access token со scope profile.read и вызывает API профиля.",
      "На интервью: важно не говорить, что OAuth — это логин сам по себе; логин и identity обычно приходят через OIDC."
    ],
    "sa-jwt": [
      "Смысл: JWT — компактный токен с claims, подписью и сроком жизни. Он удобен для stateless-проверки, но требует строгой валидации.",
      "Как применять: проверять signature, exp, aud, iss, scopes/roles; не хранить секреты в payload и учитывать отзыв токенов.",
      "Пример: API принимает JWT, проверяет issuer и audience, затем разрешает действие только при наличии scope applications.write.",
      "На интервью: частая ошибка — считать JWT зашифрованным. Обычно payload только закодирован, поэтому туда нельзя класть чувствительные данные."
    ],
    "sa-pii": [
      "Смысл: PII и персональные данные требуют минимизации, контроля доступа, маскирования, сроков хранения и аудита обработки.",
      "Как применять: классифицировать поля, фиксировать lawful basis/consent где применимо, retention, masking, encryption, export/delete scenarios и ограничения логирования.",
      "Пример: номер паспорта не должен попадать в обычные логи, а в интерфейсе поддержки показывается маскированным, если у роли нет специального разрешения.",
      "На интервью: важно показать privacy-by-design мышление: собирать только нужное, хранить ограниченно, защищать доступ и иметь аудит."
    ],
    "sa-logs": [
      "Смысл: логи фиксируют события выполнения системы и помогают расследовать инциденты, но должны быть структурированными и безопасными.",
      "Как применять: описывать уровни, обязательные поля, correlationId, user/action/resource, retention, masking PII и связь с алертами.",
      "Пример: при ошибке интеграции лог содержит integrationName, operation, correlationId, externalStatus, retryCount, но не содержит токен или полный паспорт.",
      "На интервью: проверяют, понимаешь ли ты разницу логов, метрик и трейсов, а также риски логирования чувствительных данных."
    ],
    "sa-metrics": [
      "Смысл: метрики позволяют наблюдать здоровье системы и бизнес-процесса во времени, а не искать проблему только после жалобы пользователя.",
      "Как применять: фиксировать latency, throughput, error rate, saturation, queue lag, conversion, processing time и business counters.",
      "Пример: для обработки заявок важны количество созданных заявок, время до решения, доля ошибок интеграции и размер очереди на скоринг.",
      "На интервью: важно отличать технические метрики от продуктовых и задавать пороги, владельца реакции и способ интерпретации."
    ],
    "sa-traces": [
      "Смысл: distributed tracing показывает путь одного запроса через несколько сервисов и помогает понять, где возникла задержка или ошибка.",
      "Как применять: передавать traceId/spanId между сервисами, отмечать зависимости, операции, статусы и latency каждого участка.",
      "Пример: создание заявки проходит через API Gateway, Application Service, Scoring Service и Notification Service; trace показывает, что задержка в Scoring.",
      "На интервью: полезно связать traces с correlation ID, logs и metrics как три части observability."
    ],
    "sa-contract-testing": [
      "Смысл: contract testing проверяет, что producer и consumer одинаково понимают API или событие, особенно при независимых релизах.",
      "Как применять: фиксировать ожидания consumer, схемы, примеры, обязательные поля, допустимые значения и запуск проверки в CI.",
      "Пример: потребитель ожидает поле applicationId строкой; producer не может внезапно заменить его на number без падения contract test.",
      "На интервью: важно объяснить отличие contract testing от интеграционного теста: контракт проверяет совместимость, а не полный end-to-end сценарий."
    ],
    "sa-feature-flags": [
      "Смысл: feature flags позволяют включать функциональность постепенно, отключать проблемный сценарий и разделять деплой от релиза.",
      "Как применять: описывать целевые сегменты, default state, owner, expiry date, мониторинг, rollback behavior и cleanup после стабилизации.",
      "Пример: новый расчет комиссии включается сначала для 5% пользователей, затем для региона, затем для всех, с быстрым отключением при росте ошибок.",
      "На интервью: частая ошибка — не планировать удаление флага, из-за чего система обрастает скрытыми ветками поведения."
    ],
    "sa-rollback-plan": [
      "Смысл: rollback plan описывает, как безопасно вернуться назад при проблемном релизе без потери данных и хаоса в операциях.",
      "Как применять: фиксировать условия отката, владельцев решения, совместимость данных, миграции, feature flags, коммуникации и проверку после отката.",
      "Пример: если новая версия заявки пишет дополнительное поле, rollback должен учитывать, что старая версия может не знать это поле.",
      "На интервью: важно говорить не только 'откатим деплой', а про БД, события, внешние интеграции, пользователей и поддержку."
    ]
  };

  enrich(tree, null, 0);

  function enrich(nodes, parent, depth) {
    nodes.forEach((node) => {
      node.details = byId[node.id] || buildDetails(node, parent, depth);
      if (Array.isArray(node.children)) enrich(node.children, node, depth + 1);
    });
  }

  function buildDetails(node, parent, depth) {
    const title = node.title || "Тема";
    const summary = node.summary || "Тема помогает точнее описывать требования, решения и риски.";
    const text = `${node.id} ${title} ${summary}`.toLowerCase();
    const domain = getDomain(node, parent, depth);
    const example = getExample(text, title, domain);
    const practice = getPractice(text, title, domain);
    const interview = getInterviewFocus(text, title, domain);

    return [
      `Смысл: ${summary} В базе знаний это не отдельный термин для запоминания, а рабочий инструмент аналитика: он помогает уточнить контекст, принять решение и снизить риск неверной реализации.`,
      `Как применять: ${practice}`,
      `Пример: ${example}`,
      `На интервью и в production: ${interview}`
    ];
  }

  function getDomain(node, parent, depth) {
    const id = node.id || "";
    const parentId = parent?.id || "";
    if (id.startsWith("ba-") || parentId.startsWith("ba-") || id === "business-analysis") return "ba";
    if (id.startsWith("sa-") || parentId.startsWith("sa-") || id === "system-analysis") return "sa";
    return depth <= 1 ? "domain" : "topic";
  }

  function getPractice(text, title, domain) {
    if (has(text, "stakeholder", "raci", "communication", "conflict", "executive")) return "определить участников, их интересы, влияние, право принятия решения и формат коммуникации; спорные вопросы фиксировать в decision log, а не оставлять в устных договоренностях.";
    if (has(text, "requirement", "acceptance", "traceability", "story", "use case")) return "связать цель, сценарий, правило, ограничение и критерий приемки; каждое утверждение проверить на однозначность, тестируемость и владельца.";
    if (has(text, "bpmn", "process", "workflow", "as-is", "to-be")) return "сначала описать границы процесса, роли, входы и выходы, затем основной поток, исключения, бизнес-правила, SLA и точки автоматизации.";
    if (has(text, "api", "http", "rest", "openapi", "graphql", "grpc", "soap")) return "зафиксировать контракт: endpoint/operation, параметры, схемы данных, статусы, ошибки, авторизацию, лимиты, таймауты и правила версионирования.";
    if (has(text, "integration", "async", "sync", "retries", "timeout", "idempot", "saga", "outbox", "cdc", "dlq")) return "описать тип интеграции, инициатора, контракт, гарантии доставки, повторы, идемпотентность, обработку ошибок, мониторинг и процедуру восстановления.";
    if (has(text, "sql", "database", "erd", "transaction", "index", "storage", "dictionary", "relational")) return "определить сущности, ключи, связи, правила целостности, объемы данных, частые запросы, транзакционные границы и требования к качеству данных.";
    if (has(text, "kafka", "rabbit", "broker", "event", "cqrs", "stream")) return "описать событие или сообщение как контракт: название, producer, consumer, payload, ключ, порядок, retry/DLQ, версионирование и влияние на бизнес-процесс.";
    if (has(text, "security", "oauth", "oidc", "jwt", "rbac", "abac", "pii", "audit", "threat", "secret")) return "зафиксировать субъект, ресурс, действие, модель доступа, чувствительные данные, аудит, хранение секретов и поведение системы при отказе авторизации.";
    if (has(text, "logs", "metrics", "traces", "observability", "alert", "runbook", "capacity")) return "определить сигналы здоровья системы, correlation ID, dashboards, алерты, владельцев реакции, runbook и метрики бизнес-эффекта.";
    if (has(text, "test", "release", "rollback", "migration", "feature flag")) return "связать риск с видом проверки, определить readiness criteria, тестовые данные, план релиза, мониторинг, fallback и действия поддержки.";
    if (domain === "ba") return "сформулировать проблему, участников, текущий и целевой сценарий, ограничения, критерии успеха и артефакт, который можно согласовать с бизнесом и командой.";
    return "описать границу ответственности, входы и выходы, данные, контракты, ошибки, нефункциональные требования и наблюдаемость, чтобы решение можно было реализовать и сопровождать.";
  }

  function getExample(text, title, domain) {
    if (has(text, "business model")) return "маркетплейс зарабатывает на комиссии с заказа; изменение процесса возврата влияет на продавца, покупателя, поддержку, деньги в холде и unit economics.";
    if (has(text, "kpi", "okr", "north star")) return "цель 'снизить время обработки заявки' превращается в KPI: median time to decision < 15 минут, доля ручных проверок < 20%, error rate < 1%.";
    if (has(text, "problem")) return "вместо 'нужен новый кабинет' формулируем проблему: менеджеры тратят 40% времени на поиск статуса заявки в трех системах, из-за этого растет SLA.";
    if (has(text, "business case")) return "вариант A автоматизирует только проверку данных за 2 недели, вариант B меняет весь процесс за 3 месяца; decision memo сравнивает эффект, риски и стоимость.";
    if (has(text, "capability")) return "capability 'Управление заявками' раскладывается на прием, проверку, принятие решения, уведомление и архивирование.";
    if (has(text, "interview")) return "перед интервью со стейкхолдером готовится guide: цель встречи, гипотезы, открытые вопросы, уточнения по исключениям и список решений для подтверждения.";
    if (has(text, "workshop")) return "на workshop команда сначала собирает pain points, затем группирует их, голосует за приоритеты и фиксирует next steps с владельцами.";
    if (has(text, "prototype")) return "кликабельный макет формы заявки показывает, какие поля непонятны пользователю, до того как команда начнет разработку.";
    if (has(text, "mvp")) return "MVP для скоринга может включать только создание заявки, базовую проверку и ручное решение, если это уже проверяет ценность гипотезы.";
    if (has(text, "funnel")) return "воронка onboarding: открыл форму -> заполнил данные -> подтвердил телефон -> отправил заявку; drop-off на подтверждении показывает проблему UX или SMS.";
    if (has(text, "cohort", "retention", "churn")) return "когорта клиентов, подключившихся в январе, сравнивается с февральской по retention через 7/30/90 дней.";
    if (has(text, "ab testing")) return "гипотеза: короткая форма увеличит отправку заявок; контроль видит старую форму, вариант — новую, primary metric — completion rate.";
    if (has(text, "persona")) return "персона 'оператор поддержки' имеет цель быстро понять статус клиента, ограничение по времени звонка и боль от переключения между системами.";
    if (has(text, "backlog")) return "epic 'Управление заявками' дробится на feature 'Поиск заявки', story 'Поиск по телефону' и task 'Добавить индекс по phone_hash'.";
    if (has(text, "moscow")) return "для релиза must — создать заявку и увидеть статус, should — фильтры, could — экспорт, won't — массовое редактирование.";
    if (has(text, "rice")) return "фича с большим reach, высоким impact и низким effort получает больший приоритет, но confidence снижает оценку, если данных мало.";
    if (has(text, "wsjf")) return "дорогая задержка регуляторного изменения повышает cost of delay, поэтому оно может обойти более заметную продуктовую фичу.";
    if (has(text, "glossary")) return "термин 'активный клиент' фиксируется один раз: кто считается активным, за какой период, по какому источнику данных.";
    if (has(text, "context diagram")) return "на диаграмме видно, что портал заявок общается с CRM, скорингом, DWH, сервисом уведомлений и внешним провайдером SMS.";
    if (has(text, "adr", "decision log")) return "ADR фиксирует: выбрали Kafka для событий статусов, альтернативы были REST polling и RabbitMQ, последствие — нужен schema registry.";
    if (has(text, "change impact")) return "изменение статуса заявки влияет на UI, API, отчеты, SLA поддержки, уведомления и инструкции операторов.";
    if (has(text, "rollout")) return "новый процесс запускают сначала на одном регионе, с мониторингом ошибок, обучением поддержки и планом возврата на старый поток.";
    if (has(text, "support")) return "handover включает FAQ, известные проблемы, runbook, владельцев сервисов и шаблоны ответов пользователям.";
    if (has(text, "domain model")) return "в доменной модели заявки есть Application, Applicant, Decision, ProductOffer; у заявки есть инвариант: решение нельзя принять без проверки клиента.";
    if (has(text, "ubiquitous")) return "если бизнес говорит 'анкета', разработка 'application', а поддержка 'заявка', словарь должен связать термины и выбрать основной язык.";
    if (has(text, "sequence")) return "sequence diagram показывает: UI вызывает API, API сохраняет заявку, публикует событие, скоринг возвращает решение, уведомления отправляют SMS.";
    if (has(text, "class diagram")) return "class diagram фиксирует поля Application, связи с Client и Product, ограничения cardinality и обязательность атрибутов.";
    if (has(text, "state")) return "заявка проходит Draft -> Submitted -> Scoring -> Approved/Rejected; из Rejected нельзя перейти обратно в Approved без новой проверки.";
    if (has(text, "data contract")) return "контракт события ApplicationCreated содержит applicationId, clientId, productCode, createdAt, version и correlationId.";
    if (has(text, "file exchange")) return "партнер раз в час кладет CSV в SFTP, система валидирует строки, формирует отчет ошибок и сверяет количество принятых записей.";
    if (has(text, "partial failure")) return "скоринг недоступен, но прием заявок продолжает работать: заявки получают статус WaitingForScoring и обрабатываются позже.";
    if (has(text, "consistency")) return "после создания заявки UI показывает 'обрабатывается', потому что решение появится асинхронно после события от скоринга.";
    if (has(text, "circuit")) return "если внешний сервис возвращает ошибки, circuit breaker временно прекращает вызовы и система использует fallback-сценарий.";
    if (has(text, "slo", "sla", "sli")) return "SLI: 99-й перцентиль времени ответа API; SLO: 99% запросов быстрее 500 ms; SLA: обязательство перед клиентом с последствиями нарушения.";
    if (has(text, "c4")) return "C4 Context показывает пользователей и внешние системы, Container — web app, API, DB, broker, worker, Component — внутренние модули API.";
    if (has(text, "microservice", "monolith")) return "монолит быстрее для старта и проще в транзакциях, микросервисы полезны при независимых командах, масштабировании и разных жизненных циклах.";
    if (has(text, "performance")) return "NFR: поиск заявки по номеру должен отвечать до 300 ms при 500 RPS и базе 20 млн записей.";
    if (has(text, "availability")) return "NFR: сервис приема заявок доступен 99.9%, при падении скоринга заявки принимаются в очередь без потери данных.";
    if (has(text, "dns")) return "ошибка DNS может выглядеть как недоступность API, хотя сам сервис жив; в требованиях важны timeouts и диагностика.";
    if (has(text, "tls")) return "интеграция с персональными данными требует HTTPS/TLS, проверки сертификата и запрета небезопасных протоколов.";
    if (has(text, "gateway")) return "API Gateway проверяет токен, применяет rate limit, логирует correlationId и маршрутизирует запрос в нужный сервис.";
    if (has(text, "load balancing")) return "балансировщик распределяет запросы между несколькими инстансами и исключает unhealthy-инстансы по health check.";
    if (has(text, "capacity")) return "если ожидается рост с 10 тыс. до 1 млн заявок в день, нужно оценить storage, throughput, consumer lag и пиковые часы.";
    if (has(text, "migration")) return "миграция добавляет поле status_reason; план включает backfill, совместимость старой версии API и проверку отчетов.";
    if (has(text, "release checklist")) return "перед релизом проверяют миграции, feature flags, мониторинг, инструкции поддержки, rollback и владельца решения go/no-go.";
    if (has(text, "ai")) return "AI может предложить черновик требований или схемы API, но аналитик проверяет факты, контекст, терминологию, безопасность и соответствие source of truth.";
    return domain === "ba" ? `для темы '${title}' можно взять реальный рабочий процесс, описать текущую боль, участников, правила, целевой результат и критерии приемки изменения.` : `для темы '${title}' можно взять сценарий создания заявки, описать входные данные, контракт, состояние, ошибки, интеграции, мониторинг и ограничения эксплуатации.`;
  }

  function getInterviewFocus(text, title, domain) {
    if (has(text, "api", "http", "rest", "openapi")) return "ожидают понимания контракта, идемпотентности, статусов, ошибок, пагинации, версионирования и влияния изменений на потребителей.";
    if (has(text, "kafka", "rabbit", "event", "broker")) return "ожидают понимания producer/consumer, гарантий доставки, порядка, повторов, DLQ, версионирования схем и eventual consistency.";
    if (has(text, "sql", "database", "transaction", "index", "erd")) return "ожидают умения читать данные, проектировать сущности, объяснять связи, транзакции, индексы и ограничения целостности.";
    if (has(text, "security", "oauth", "jwt", "pii", "audit")) return "ожидают различения authentication/authorization, понимания токенов, ролей, персональных данных, аудита и угроз.";
    if (has(text, "bpmn", "process")) return "ожидают корректной нотации, умения показать исключения, роли, границы процесса и точки автоматизации.";
    if (has(text, "requirement", "story", "use case", "acceptance")) return "ожидают конкретики: кто пользователь, какая ценность, какие правила, что считается готовым и как проверить результат.";
    if (has(text, "observability", "logs", "metrics", "traces", "alert")) return "ожидают понимания, как система будет диагностироваться после релиза, кто реагирует на алерты и какие сигналы важны.";
    if (has(text, "release", "rollback", "test", "migration")) return "ожидают умения связать требования с проверками, rollout, rollback, тестовыми данными и операционной готовностью.";
    return domain === "ba" ? "важно отвечать через цель, контекст, стейкхолдеров, артефакт, критерии проверки и пример из практики; слабый ответ звучит как пересказ термина без применения." : "важно отвечать через границы системы, контракт, данные, ошибки, эксплуатацию и trade-offs; слабый ответ перечисляет технологии без объяснения последствий.";
  }

  function has(text, ...needles) {
    return needles.some((needle) => text.includes(needle));
  }
})();
